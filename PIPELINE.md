# 隧道点云标靶球自动检测方法

## 一、技术领域与问题

本发明涉及激光扫描点云中球形反光标靶的自动定位。隧道施工测量中，在隧道内布设反光涂层标靶球（直径 0.2m），通过三维激光扫描获取点云数据，需自动检出每个标靶球的三维坐标。

标靶球固定在三脚架上架设于地面。激光回波中，球面反光涂层产生高强度信号（hi），三脚架金属腿产生低强度信号（lo），地面则位于最底层。本方法利用这三层物理结构实现鲁棒检测。

## 二、整体流程

输入：单帧隧道 PCD 点云（每点含 x, y, z, intensity 四字段）。
输出：每个标靶球的三维坐标 (cx, cy, cz)，按置信度排序。

算法分三层串行：**候选生成 → 特征提取 → 评分决策**。

```
PCD 点云
    │
    ▼
┌────────────── 层一：候选生成 ──────────────┐
│ 强度高通 → Z 切片 → XY 投影聚类 → 合并     │
│ → 圆柱裁剪 → 地面删除 → dxy 收敛迭代        │
└──────────────────┬────────────────────────┘
                   ▼ 候选球位 (cx, cy, cz)
┌────────────── 层二：特征提取 ──────────────┐
│ ratio(强度双峰比)  tripod(角分布质量)        │
│ compact(空间紧密度) dxy(定位精度)           │
│ ground(地面信号)                            │
└──────────────────┬────────────────────────┘
                   ▼ 五维特征向量
┌────────────── 层三：评分决策 ──────────────┐
│ 门控过滤 → 加权评分 → 空间去重 → 排序       │
└──────────────────┬────────────────────────┘
                   ▼
           检测结果列表 (按置信度降序)
```

## 三、层一：候选球位生成

### 3.1 强度高通滤波

激光回波强度反映材质反射率。球面反光涂层回波强度显著高于隧道壁和地面。

取全点云强度排序，保留第 P 百分位以上的点（默认 P=80），滤除低强度背景噪声。阈值 τ_I 定义为：

$$\tau_I = I_{\lfloor N \cdot P/100 \rfloor}$$

其中 N 为总点数，I 为排序后的强度序列。

### 3.2 Z 轴分层切片

隧道沿 Z 轴（高程方向）延伸。标靶球架设高度通常在 -5m 至 3m（相对路面）。沿 Z 轴以步长 Δz（默认 0.2m）对高通点云进行切片。对每个切片 [z, z+Δz]：

1. 取出该层内所有高通点
2. 将三维点投影到 XY 平面（丢弃 Z 坐标）
3. 在二维投影上进行欧氏距离聚类

### 3.3 XY 平面聚类

采用 KdTree 加速的欧氏聚类算法。两点 XY 距离小于容差 ε（默认 0.15m）时归入同一簇。

对每个簇施加尺度约束：簇的 XY 对角线长度须在 [0.3D, 3D] 区间，其中 D 为标靶球直径（默认 0.2m）。此约束排除过大（隧道壁连续面）和过小（孤立噪点）的伪候选。

满足尺度约束的簇，取其 XY 质心 (cx, cy) 和平均 Z 值 cz 作为该切片层的候选。

### 3.4 跨层合并

同一标靶球在相邻 Z 切片层中会产生位置相近的候选。对来自不同层的候选进行图连通合并：

- XY 距离 < 0.3m 且 |Δz| < 0.5m 的两个候选视为同一标靶球
- 以簇大小为权重，计算合并后的强度加权质心 (cx, cy, cz)

此步骤将多层碎片化候选聚合为单一球位估计。

### 3.5 圆柱空间裁剪

以候选球位 (cx, cy) 为中心、半径 R_cyl（默认 0.5m）建立垂直圆柱裁剪区域。同时限制 Z 方向范围 [cz - D_dn, cz + D_up]（默认向下 2.0m、向上 0.3m）。

圆柱内包含了完整的标靶球、三脚架和局部地面的点云数据。后续所有操作均在此裁剪区域内进行。

### 3.6 Z-base 地面删除

地面是标靶球下方的水平面，会干扰特征计算。采用两步法删除：

**步骤一**：取裁剪区域内 Z 值最低的 10% 点，计算其平均 Z 值 μ_z。删除所有 Z 值在 [μ_z - δ, μ_z + δ] 内的点（δ = ransac_distance = 0.05m）。这删除了水平地面层。

**步骤二**：对裁剪区域外环（半径 [0.35, 0.50]m）内的点进行 RANSAC 平面拟合（1000 次迭代）。若拟合平面的法向量 Z 分量 n_z < 0.3，判定为竖直墙面，删除该平面上 δ 距离内的全部点。

物理依据：三脚架立在水平地面上→地面点 Z 值最低且集中；隧道壁为竖直面→法向量接近水平。外环排除了中心球体区域，避免将球面误识别为墙面。

### 3.7 dxy 驱动的迭代重定位

由于强度高通滤波、Z 切片离散化、地面删除等步骤，初始候选球位 (cx, cy) 可能与真实球心存在偏移。引入迭代修正机制：

1. 在当前 (cx, cy) 处执行 3.5-3.6（裁剪+去地面）
2. 对裁剪区域内点按强度进行 k=2 一维 k-means 聚类，分为低强度组 lo（三脚架+地面残余）和高强度组 hi（球面反光点）
3. 分别计算 lo 和 hi 的 XY 质心
4. 计算二者质心间距 dxy = ‖C_hi - C_lo‖_XY
5. 若 dxy > τ_recenter（默认 0.15m），将 (cx, cy) 修正为 lo 和 hi 质心的中点，返回步骤 1
6. 否则收敛，或达到最大迭代次数（默认 3 次）时退出

物理依据：理想情况下，球心应在三脚架中心正上方，hi 与 lo 的 XY 质心趋于重合。若存在偏移，说明裁剪中心不准，向中点移动可逼近真实球心。

收敛后的 dxy 作为层二的特征之一。

## 四、层二：五维特征向量

### 4.1 强度双峰比（ratio）

裁剪区域内的点云呈现天然双峰强度分布：低强度峰对应三脚架和地面（lo），高强度峰对应球面反光涂层（hi）。

对裁剪区域内点按强度排序，以最小化组内方差为目标，搜索最优二分点 k*：

$$k^* = \arg\min_k \left[ \sum_{i=0}^{k-1}(I_i - \bar{I}_{lo})^2 + \sum_{i=k}^{N-1}(I_i - \bar{I}_{hi})^2 \right]$$

其中搜索范围限制在 [0.1N, 0.9N] 以避免极端划分。

ratio 定义为高强度组均值与低强度组均值之比：

$$\text{ratio} = \frac{\bar{I}_{hi}}{\bar{I}_{lo} + \epsilon}$$

ε = 0.1 为防止除零的平滑项。ratio 值越大，说明物理双峰结构越显著，标靶球存在的可能性越高。

### 4.2 三脚架角分布（tripod）

三脚架三条腿在 XY 平面投影中形成三个大致等间距（约 120°）的方向簇。从 lo 点（低强度组）计算：

1. 以 lo 质心为原点，计算每个 lo 点的 XY 方位角 θ_i = atan2(y_i - C_lo_y, x_i - C_lo_x)
2. 将方位角离散化为 B 个等宽区间（默认 36 个，即每 10° 一个 bin），构建角度直方图
3. 遍历直方图，标记局部最大值（高于相邻 bin 且高于均值 × 1.5）为峰
4. 取最高的 3 个峰，检查两两间距是否接近 120°（容差 ±20°）
5. 得分 = 间距均匀度 × 峰高均匀度（最低峰 / 最高峰）

$$\text{tripod} = \left(1 - \frac{\bar{e}_\theta}{\theta_{tol}}\right)_+ \cdot \frac{h_{min}}{h_{max}}$$

其中 ē_θ 为三个峰间距与理想 120° 的平均偏差，θ_tol = 20°。

物理依据：三脚架在 XY 平面形成三条辐射状腿，角度天然隔离。相比 3D 聚类（lo 点密度不足），2D 角分布在稀疏数据上更为鲁棒。

### 4.3 球面紧密度（compact）

标靶球的反光涂层形成半球面。取 hi 点中 Z 值最高的点 Z_max，在 [Z_max - W, Z_max] 窗口内（W = 0.24m）提取球面候选点，计算其三维空间方差 σ²：

$$\sigma^2 = \frac{1}{M}\sum_{j=1}^{M} \|P_j - \bar{P}\|^2$$

$$\text{compact} = \frac{1}{1 + \sigma}$$

其中 M 为窗口内点数，P̄ 为质心。compact ∈ (0, 1]，值越大表示点越集中，球面形态越好。物理上限 W = 0.24m 略大于球半径 0.1m，留有余量容纳球面曲率。

### 4.4 定位偏差（dxy）

取层一迭代收敛后的 lo/hi 质心 XY 距离：

$$\text{dxy} = \|C_{hi} - C_{lo}\|_{XY}$$

dxy 越小，球心与三脚架中心越对齐，定位越精确。

### 4.5 地面信号（ground）

$$\text{ground} = \frac{N_{removed}}{N_{original}}$$

即地面删除步骤中移除的点数与原始裁剪区域点数之比。若地面信号过弱（< 0.05），说明该候选可能悬浮在空中（误检），或者是特殊场景无可见地面。

## 五、层三：门控、评分与决策

### 5.1 门控过滤

对每个候选施加五点硬性门控（AND 逻辑）：

| 条件 | 物理含义 |
|------|---------|
| ratio ≥ 5.0 | 必须有显著双峰强度分布 |
| compact ≥ 0.88 | 球面点必须足够集中 |
| dxy ≤ 0.15m | 球心与架心偏移不超过球半径 |
| ground ≥ 0.05 | 必须检测到地面信号 |
| tripod ≥ 0（可选） | 三脚架结构存在（仅加权，不硬挡） |

注意 tripod 不计入 gate。原因是三脚架数据质量受灰尘、遮挡影响大，作为硬性门槛会造成漏检。tripod 仅通过评分权重影响排序。

### 5.2 加权评分

通过门控的候选按五维特征加权求和：

$$S = w_t \cdot \text{tripod} + w_r \cdot \text{ratio} + w_c \cdot \text{compact} - w_d \cdot \text{dxy} + w_g \cdot \text{ground}$$

默认权重：(w_t, w_r, w_c, w_d, w_g) = (4, 1, 2, 3, 3)。tripod 权重最高（4.0），体现三脚架物理约束的重要性。dxy 取负权重（-3.0），偏移越大扣分越多。

### 5.3 空间去重

同一标靶球可能被多个候选框捕获。对 XY 距离 < 0.5m 且 |Δz| < 1.0m 的候选对，保留 dxy 更小的那个（定位更精确）。去重后按 S 降序排列。

## 六、参数默认值及物理依据

| 参数 | 默认值 | 依据 |
|------|--------|------|
| 强度百分位 P | 80 | 反光涂层约占扫描点 5-15%，取 P80 确保不漏 |
| Z 切片范围 | 3.0 → -5.0m | 覆盖典型隧道断面高度 |
| Z 步长 | 0.2m | 略小于球直径，确保每个球至少命中 1-2 层 |
| 聚类容差 ε | 0.15m | 小于标靶间最小间距，防止跨标靶合并 |
| 圆柱半径 R_cyl | 0.5m | 三脚架跨度 + 安全余量 |
| Z-buffer 向下/向上 | 2.0 / 0.3m | 向下覆盖完整三脚架高度，向上覆盖球顶 |
| 外环墙面检测 | [0.35, 0.50]m | 排除中心球区，只取外围可能墙面区域 |
| 迭代次数上限 | 3 | 实测 1-2 次即收敛，上限防死循环 |
| dxy 收敛阈值 | 0.15m | 小于球半径，超过时球位明显不准 |
| compact Z 窗口 | 0.24m | 球体 0.2m + 0.04m 曲面余量 |
| ratio 门控阈值 | 5.0 | 确保至少 5:1 强度对比 |
| compact 门控 | 0.88 | 经验阈值，球面半方差对应 |
| 去重 XY 半径 | 0.5m | 标靶间最小布设距离 |
| 去重 Z 范围 | 1.0m | 允许同一标靶在不同切片被检出 |

## 七、新颖性要点

1. **dxy 驱动的迭代重定位**：利用 hi/lo 双模质心偏移作为反馈信号，驱动裁剪窗口自动修正至最优球位，无需人工标定。
2. **二维角分布 tripod 特征**：将稀疏 lo 点投影至 XY 平面进行角度直方图分析，克服了 3D 聚类 lo 点密度不足的固有困难。
3. **Z-base 地面删除 + 外环 RANSAC 墙面检测**：利用圆柱几何约束分离地面和墙面，避免错误删除球面点。
4. **强度一维 k-means 双峰比**：以最小化组内方差为优化目标自动寻找最优强度二分点，适应不同扫描仪和场景的强度分布。
5. **跨 Z 切片图连通合并**：将三维检测降维为二维聚类后，再通过图连通性恢复三维一致性，兼顾计算效率和定位精度。
